# Execution-engine experiment daily digest — workload LAND; TD-3383 design released — 2026-08-06

```text
2026-08-06 / fenic-exec-engine-breakdown-v1 / FULL ADMITTED CHARTER COMPLETE: BASE STACK COMPLETE; VALIDATION REPORT ACCEPTED; TD-3334 NO-OP ACCEPTED; TD-3384 LAND; TD-3385 LAND-WITH-FOLLOWUPS
receipts: every ratified branch is pushed and LAND. Final B1 receipt chain is 2ee449b (Herd LAND-WITH-FOLLOWUPS) -> bd7d89b (directed test/docs closure, no separate review round). Final fake/local gate: 45 passed; ruff and diff checks clean.
memory: all bounds are working-set contracts, not blanket RSS-win claims. B0 does not bound final operator output; B1 does not bound final projection materialization. B1’s deterministic idle comparison is 30 ns serial -> 0 ns fused handoff.
correctness: P1c’s DO-NOT-LAND caught real cross-side lineage corruption and silent one-pair token-budget acceptance; delta LAND confirmed both fixes. B1 documents and tests its deliberate prior-extract-dispatch-before-later-map-abort divergence.
idle: P0 metric semantics remain authoritative: queue/rate-limit separate; retry backoff excluded. B1 now asserts its full lifecycle event sequence.
validation: Herd accepted the original honest HOLD, authorized one Amendment A, and accepted the final report. The hardened harness persisted all seven synthetic raw/normalized comparisons, LMMetrics, and lifecycle event receipts before asserting. Full matrix parity passed: fusion wall time was 55.65% faster at 64 rows and 46.54% faster at 160, but 2.58% slower at 320; this is a conditional signal, not a universal throughput claim. Join 16×16 returned 64/64 expected survivors in 7,614.994 ms at 304,168,960 B peak RSS.
idle: P0 metric treatment stayed intact. There were 134 lifecycle `rate_limited` transitions, but zero retried/failed events and zero excluded rate-limited idle ns; no provider 429/backoff claim is made. Fusion idle gap improved at 64/160 and regressed at 320, matching the mixed wall result.
spend: original stopped arm remains a $0.041574 conservative upper bound. Amendment A's serialized LMMetrics report $0.000000 tokens/requests/cost despite non-empty lifecycle events, a telemetry limitation not a free-provider claim. Cumulative conservative planning bound $0.785305, below $50.
TD-3334 pre-check: the $0 fake/local regression test passed (2 passed): B0's `iter_completions` path and legacy `get_completions` both accumulate deterministic LMMetrics, while a distinct unused client remains zero. No iterator-path metrics fix card is warranted; the live zero metrics are retained as a provider/telemetry limitation.
TD-3334 survey: Herd accepted the evidence-backed no-op. The canonical typedef data-intelligence sync paths contain a 12-pass logical sequence, but every pass is immediately `cache()`d and `count()`ed for lazy-evaluation error visibility. Each physical plan contains at most one semantic operation, so B1 covers production today. This is not permanent: TD-4372 and TD-3383 are intentionally narrowing pass gaps. TD-3334's concrete revisit trigger is the first unmaterialized production multi-op chain they create, with this validation matrix as the sizing prior; it remains open in Linear with those relations.
TD-3384 audit: Herd LANDed `f0fcc14`, no Majors, with independent cross-model provenance and a 35-pass focused gate. The local 4,096-row × 512-column measurement found ingestion coercion 0.960354 -> 0.350979 ms median, same-order union 0.869271 -> 0.267583 ms, and identity projection 0.431937 -> 0.137688 ms (40 timed rounds, fixture construction excluded). Directed tests cover same-name aliases, casts, regex selectors, reorderings, computed expressions, and normalized datetime/nested-Struct/EmbeddingType frames. Source/sink work remains I/O/side-effectful or already benefits through ingestion; alias restore was only 0.034583 ms and is retained.
process: an over-broad local test selection reached one non-fake-injected semantic-metrics test. The known placeholder key was rejected with 401 before any successful provider response; no `.env` credential was read. The command was stopped and no further provider-capable tests will run. This is a test-selection incident, not a billing receipt or product failure.
process: TD-3384's review pinned `f0fcc14`; its prior docs-only `fd11035` had identical `src`/`tests`, so Herd accepted it. Going forward, all freeze-request records are committed before the pinned review dispatch and the reviewed branch is not advanced afterward. The present minor closure is directly authorized by the LAND verdict and needs no new round.
TD-3385 benchmark: Herd LAND-WITH-FOLLOWUPS accepted the local-only no-op. On dense Float32 fixtures, current NumPy beat native Polars for normalize and every similarity form at 384 and 1,536 dimensions (native was 10% to 583% slower). The proposed component-wise native average was 4.80x slower at 384 dimensions because it expands one aggregate expression per component. Native dot also failed the predeclared tight numerical-parity guard: pairwise 384-D results differed in 30,104/32,768 rows, max absolute delta 0.000057220458984375. Null/zero/dtype boundary probes passed, but no candidate is a clear parity-preserving win; retain NumPy. A provider-free focused gate passed 18 tests.
TD-3385 reproducibility: the committed seeded local harness and raw per-round JSON receipts live in `.context/validation/td3385-evidence/`; an independent query-dot receipt reports 29,998 differing rows at the same maximum absolute delta. The standing `.context/PROVIDER-FREE-TEST-SELECTION.md` now requires reviewed exact-node allowlists and excludes known provider-capable metrics, embedding, reader, sim-join, and semantic-join tests. A placeholder key is fixture-construction only, not a network permit.
process: TD-3385's first broad selector mistakenly included three provider-backed embedding tests. The placeholder key received 401 before any successful response; no `.env` credential was read or charge incurred. The selector was stopped and excluded; no provider-capable test was rerun. The new standing rule closes this recurring incident class.
charter: the full admitted charter is complete: validation/report, TD-3334 no-op, TD-3384 fast paths, and TD-3385 no-op. Captain's later workload extension is now LAND-WITH-FOLLOWUPS after Herd's independent review and its directly authorized test/docs closure. The $0 real-clock, simulated-ModelClient 12-step matrix completed all 12 receipts (2,674.807 active seconds, 25.193 under its 45-minute budget). The matrix itself verified row IDs, overlay structs, per-step logical totals, and actual output-token draws; the new focused retained-column proof verifies step 01–11 content equality across all arms before final projection. Every receipt recorded `[false, false, true]` `used_fusion`, and runtime matrix assertions now enforce it: the ≤0.09% B1 result is engaged, no effect in this governor-bound shape. The canonical 192-row expansion was 415,002 versus the 150,000-token bucket (106.001s ideal refill); observed matching arms were 107.5–113.1s and no watchdog fired. Barrier relaxation varied by lane; modest simulated overshoot raised 429/retry counts and wall while P0 non-rate idle stayed <=0.238s. A matrix third-seed reduction branch was initially checked between lanes; the visible partial manifest was retained, code corrected, and a single bounded final simulated lane resumed. Its historical reduction object was already overwritten by the prior code and is not reconstructed; the reusable resume path now validates the exact missing receipt set and preserves future reduction objects under `superseded_reduction`. No provider call or spend occurred. TD-3383 is released to its separate local-only design-first gate.
```

The current Captain-facing report is
`.context/outbox/fenic-exec-engine-validation-report-2026-08-06.md`.
