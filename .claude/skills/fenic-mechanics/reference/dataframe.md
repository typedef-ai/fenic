<!-- GENERATED by generate_reference.py against fenic==0.11.0. Do not edit by hand; re-run the generator. -->
<!-- fenic version: 0.11.0 -->

# Session / DataFrame / Column methods reference (generated)

## Session

- `Session.createDataFrame(self, data: 'DataLike', schema: 'Schema | None' = None) -> 'DataFrame'` — Create a DataFrame from a variety of Python-native data formats.
- `Session.create_dataframe(self, data: 'DataLike', schema: 'Schema | None' = None) -> 'DataFrame'` — Create a DataFrame from a variety of Python-native data formats.
- `Session.getOrCreate(config: 'SessionConfig') -> 'Session'` — Gets an existing Session or creates a new one with the configured settings.
- `Session.get_or_create(config: 'SessionConfig') -> 'Session'` — Gets an existing Session or creates a new one with the configured settings.
- `Session.sql(self, query: 'str', /, **tables: 'DataFrame') -> 'DataFrame'` — Execute a read-only SQL query against one or more DataFrames using named placeholders.
- `Session.stop(self, skip_usage_summary: 'bool' = False)` — Stops the session and closes all connections.
- `Session.table(self, table_name: 'str') -> 'DataFrame'` — Returns the specified table as a DataFrame.
- `Session.view(self, view_name: 'str') -> 'DataFrame'` — Returns the specified view as a DataFrame.

## DataFrame

- `DataFrame.agg(self, *exprs: 'Union[Column, Dict[str, str]]') -> 'DataFrame'` — Aggregate on the entire DataFrame without groups.
- `DataFrame.cache(self) -> 'DataFrame'` — Alias for persist(). Mark DataFrame for caching after first computation.
- `DataFrame.collect(self, data_type: 'DataLikeType' = 'polars') -> 'QueryResult'` — Execute the DataFrame computation and return the result as a QueryResult.
- `DataFrame.count(self) -> 'int'` — Count the number of rows in the DataFrame.
- `DataFrame.distinct(self) -> 'DataFrame'` — Return a DataFrame with duplicate rows removed. Alias for drop_duplicates(subset=None).
- `DataFrame.drop(self, *col_names: 'str') -> 'DataFrame'` — Remove one or more columns from this DataFrame.
- `DataFrame.dropDuplicates(self, subset: 'Optional[List[str]]' = None) -> 'DataFrame'` — Return a DataFrame with duplicate rows removed.
- `DataFrame.drop_duplicates(self, subset: 'Optional[List[str]]' = None) -> 'DataFrame'` — Return a DataFrame with duplicate rows removed.
- `DataFrame.explain(self) -> 'None'` — Display the logical plan of the DataFrame.
- `DataFrame.explode(self, column: 'ColumnOrName') -> 'DataFrame'` — Create a new row for each element in an array column.
- `DataFrame.explode_outer(self, column: 'ColumnOrName') -> 'DataFrame'` — Create a new row for each element in an array column, containing the element's position in the array and its value, and preserving null/empty arrays.
- `DataFrame.explode_with_index(self, column: 'ColumnOrName', index_col_name: 'str' = 'pos', value_col_name: 'str' = 'col', keep_null_and_empty: 'bool' = False) -> 'DataFrame'` — Create a new row for each element in an array column, with the element's position in the array and its value.
- `DataFrame.filter(self, condition: 'Column') -> 'DataFrame'` — Filters rows using the given condition.
- `DataFrame.groupBy(self, *cols: 'ColumnOrName') -> 'GroupedData'` — Groups the DataFrame using the specified columns.
- `DataFrame.group_by(self, *cols: 'ColumnOrName') -> 'GroupedData'` — Groups the DataFrame using the specified columns.
- `DataFrame.groupby(self, *cols: 'ColumnOrName') -> 'GroupedData'` — Groups the DataFrame using the specified columns.
- `DataFrame.join(self, other: 'DataFrame', on: 'Optional[Union[str, List[str]]]' = None, *, left_on: 'Optional[Union[ColumnOrName, List[ColumnOrName]]]' = None, right_on: 'Optional[Union[ColumnOrName, List[ColumnOrName]]]' = None, how: 'JoinType' = 'inner') -> 'DataFrame'` — Joins this DataFrame with another DataFrame.
- `DataFrame.limit(self, n: 'int') -> 'DataFrame'` — Limits the number of rows to the specified number.
- `DataFrame.lineage(self) -> 'Lineage'` — Create a Lineage object to trace data through transformations.
- `DataFrame.orderBy(self, cols: 'Union[ColumnOrName, List[ColumnOrName], None]' = None, ascending: 'Optional[Union[bool, List[bool]]]' = None) -> 'DataFrame'` — Sort the DataFrame by the specified columns. Alias for sort().
- `DataFrame.order_by(self, cols: 'Union[ColumnOrName, List[ColumnOrName], None]' = None, ascending: 'Optional[Union[bool, List[bool]]]' = None) -> 'DataFrame'` — Sort the DataFrame by the specified columns. Alias for sort().
- `DataFrame.persist(self) -> 'DataFrame'` — Mark this DataFrame to be persisted after first computation.
- `DataFrame.posexplode(self, column: 'ColumnOrName') -> 'DataFrame'` — Create a new row for each element in an array column, with the element's position in the array and its value.
- `DataFrame.posexplode_outer(self, column: 'ColumnOrName') -> 'DataFrame'` — Create a new row for each element in an array column with position and value, preserving null/empty arrays.
- `DataFrame.select(self, *cols: 'ColumnOrName') -> 'DataFrame'` — Projects a set of Column expressions or column names.
- `DataFrame.show(self, n: 'int' = 10, explain_analyze: 'bool' = False) -> 'None'` — Display the DataFrame content in a tabular form.
- `DataFrame.sort(self, cols: 'Union[ColumnOrName, List[ColumnOrName], None]' = None, ascending: 'Optional[Union[bool, List[bool]]]' = None) -> 'DataFrame'` — Sort the DataFrame by the specified columns.
- `DataFrame.to_arrow(self) -> 'pa.Table'` — Execute the DataFrame computation and return an Apache Arrow Table.
- `DataFrame.to_pandas(self) -> 'pd.DataFrame'` — Execute the DataFrame computation and return a Pandas DataFrame.
- `DataFrame.to_polars(self) -> 'pl.DataFrame'` — Execute the DataFrame computation and return the result as a Polars DataFrame.
- `DataFrame.to_pydict(self) -> 'Dict[str, List[Any]]'` — Execute the DataFrame computation and return a dictionary of column arrays.
- `DataFrame.to_pylist(self) -> 'List[Dict[str, Any]]'` — Execute the DataFrame computation and return a list of row dictionaries.
- `DataFrame.union(self, other: 'DataFrame') -> 'DataFrame'` — Return a new DataFrame containing the union of rows in this and another DataFrame.
- `DataFrame.unionAll(self, other: 'DataFrame') -> 'DataFrame'` — Return a new DataFrame containing the union of rows in this and another DataFrame.
- `DataFrame.unnest(self, *col_names: 'str') -> 'DataFrame'` — Unnest the specified struct columns into separate columns.
- `DataFrame.where(self, condition: 'Column') -> 'DataFrame'` — Filters rows using the given condition (alias for filter()).
- `DataFrame.withColumn(self, col_name: 'str', col: 'Union[Any, Column, pl.Series, pd.Series]') -> 'DataFrame'` — Add a new column or replace an existing column.
- `DataFrame.withColumnRenamed(self, col_name: 'str', new_col_name: 'str') -> 'DataFrame'` — Rename a column. No-op if the column does not exist.
- `DataFrame.withColumns(self, cols_map: 'Dict[str, Union[Any, Column, pl.Series, pd.Series]]') -> 'DataFrame'` — Add multiple new columns or replace existing columns.
- `DataFrame.with_column(self, col_name: 'str', col: 'Union[Any, Column, pl.Series, pd.Series]') -> 'DataFrame'` — Add a new column or replace an existing column.
- `DataFrame.with_column_renamed(self, col_name: 'str', new_col_name: 'str') -> 'DataFrame'` — Rename a column. No-op if the column does not exist.
- `DataFrame.with_columns(self, cols_map: 'Dict[str, Union[Any, Column, pl.Series, pd.Series]]') -> 'DataFrame'` — Add multiple new columns or replace existing columns.

## Column

- `Column.alias(self, name: 'str') -> 'Column'` — Create an alias for this column.
- `Column.asc(self) -> 'Column'` — Mark this column for ascending sort order.
- `Column.asc_nulls_first(self) -> 'Column'` — Alias for asc().
- `Column.asc_nulls_last(self) -> 'Column'` — Mark this column for ascending sort order with nulls last.
- `Column.cast(self, data_type: 'DataType') -> 'Column'` — Cast the column to a new data type.
- `Column.contains(self, other: 'Union[str, Column]') -> 'Column'` — Check if the column contains a substring.
- `Column.contains_any(self, others: 'List[str]', case_insensitive: 'bool' = True) -> 'Column'` — Check if the column contains any of the specified substrings.
- `Column.desc(self) -> 'Column'` — Mark this column for descending sort order.
- `Column.desc_nulls_first(self) -> 'Column'` — Alias for desc().
- `Column.desc_nulls_last(self) -> 'Column'` — Mark this column for descending sort order with nulls last.
- `Column.ends_with(self, other: 'Union[str, Column]') -> 'Column'` — Check if the column ends with a substring.
- `Column.getItem(self, key: 'Union[str, int, Column]') -> 'Column'` — Access an item in a struct or array column.
- `Column.get_item(self, key: 'Union[str, int, Column]') -> 'Column'` — Access an item in a struct or array column.
- `Column.ilike(self, other: 'Union[str, Column]') -> 'Column'` — Check if the column matches a SQL LIKE pattern (case-insensitive).
- `Column.is_in(self, other: 'Union[List[Any], ColumnOrName]') -> 'Column'` — Check if the column is in a list of values or a column expression.
- `Column.is_not_null(self) -> 'Column'` — Check if the column contains non-NULL values.
- `Column.is_null(self) -> 'Column'` — Check if the column contains NULL values.
- `Column.like(self, other: 'Union[str, Column]') -> 'Column'` — Check if the column matches a SQL LIKE pattern.
- `Column.otherwise(self, value: 'Column') -> 'Column'` — Returns a value when no prior conditions are True.
- `Column.rlike(self, other: 'Union[str, Column]') -> 'Column'` — Check if the column matches a regular expression pattern.
- `Column.starts_with(self, other: 'Union[str, Column]') -> 'Column'` — Check if the column starts with a substring.
- `Column.when(self, condition: 'Column', value: 'Column') -> 'Column'` — Evaluates a condition for each row and returns a value when true.
