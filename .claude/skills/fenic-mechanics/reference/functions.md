<!-- GENERATED by generate_reference.py against fenic==0.9.0. Do not edit by hand; re-run the generator. -->
<!-- fenic version: 0.9.0 -->

# fenic functions reference (generated)

## fc.text.\*

- `fc.text.array_join(column: Union[Column, str], delimiter: str) -> Column` — Joins an array of strings into a single string with a delimiter.
- `fc.text.btrim(col: Union[Column, str], trim: Union[Column, str, None]) -> Column` — Remove specified characters from both sides of strings in a column.
- `fc.text.byte_length(column: Union[Column, str]) -> Column` — Calculate the byte length of each string in the column.
- `fc.text.character_chunk(column: Union[Column, str], chunk_size: int, chunk_overlap_percentage: int = 0) -> Column` — Chunks a string column into chunks of a specified size (in characters) with an optional overlap.
- `fc.text.compute_fuzzy_ratio(column: Union[Column, str], other: Union[Column, str], method: Literal['indel', 'levenshtein', 'damerau_levenshtein', 'jaro_winkler', 'jaro', 'hamming'] = 'indel') -> Column` — Compute the similarity between two strings using a fuzzy string matching algorithm.
- `fc.text.compute_fuzzy_token_set_ratio(column: Union[Column, str], other: Union[Column, str], method: Literal['indel', 'levenshtein', 'damerau_levenshtein', 'jaro_winkler', 'jaro', 'hamming'] = 'indel') -> Column` — Compute fuzzy similarity using token set comparison.
- `fc.text.compute_fuzzy_token_sort_ratio(column: Union[Column, str], other: Union[Column, str], method: Literal['indel', 'levenshtein', 'damerau_levenshtein', 'jaro_winkler', 'jaro', 'hamming'] = 'indel') -> Column` — Compute fuzzy similarity after sorting tokens in each string.
- `fc.text.concat(*cols: Union[Column, str]) -> Column` — Concatenates multiple columns or strings into a single string.
- `fc.text.concat_ws(separator: str, *cols: Union[Column, str]) -> Column` — Concatenates multiple columns or strings into a single string with a separator.
- `fc.text.count_tokens(column: Union[Column, str]) -> Column` — Returns the number of tokens in a string using OpenAI's cl100k_base encoding (tiktoken).
- `fc.text.extract(column: Union[Column, str], template: str) -> Column` — Extracts structured data from text using template-based pattern matching.
- `fc.text.jinja(jinja_template: str, /, strict: bool = True, **columns: Column) -> Column` — Render a Jinja template using values from the specified columns.
- `fc.text.length(column: Union[Column, str]) -> Column` — Calculate the character length of each string in the column.
- `fc.text.lit(value: Any) -> Column` — Creates a Column expression representing a literal value.
- `fc.text.lower(column: Union[Column, str]) -> Column` — Convert all characters in a string column to lowercase.
- `fc.text.ltrim(col: Union[Column, str]) -> Column` — Remove whitespace from the start of strings in a column.
- `fc.text.parse_transcript(column: Union[Column, str], format: Literal['srt', 'generic', 'webvtt']) -> Column` — Parses a transcript from text to a structured format with unified schema.
- `fc.text.recursive_character_chunk(column: Union[Column, str], chunk_size: int, chunk_overlap_percentage: int, chunking_character_set_custom_characters: Optional[list[str]] = None) -> Column` — Chunks a string column into chunks of a specified size (in characters) with an optional overlap.
- `fc.text.recursive_token_chunk(column: Union[Column, str], chunk_size: int, chunk_overlap_percentage: int, chunking_character_set_custom_characters: Optional[list[str]] = None) -> Column` — Chunks a string column into chunks of a specified size (in tokens) with an optional overlap.
- `fc.text.recursive_word_chunk(column: Union[Column, str], chunk_size: int, chunk_overlap_percentage: int, chunking_character_set_custom_characters: Optional[list[str]] = None) -> Column` — Chunks a string column into chunks of a specified size (in words) with an optional overlap.
- `fc.text.regexp_count(src: Union[Column, str], pattern: Union[Column, str]) -> Column` — Count the number of times a regex pattern is matched in a string.
- `fc.text.regexp_extract(src: Union[Column, str], pattern: Union[Column, str], idx: int) -> Column` — Extract a specific regex group from a string.
- `fc.text.regexp_extract_all(src: Union[Column, str], pattern: Union[Column, str], idx: Union[Column, int] = 0) -> Column` — Extract all strings matching a regex pattern, optionally from a specific group.
- `fc.text.regexp_instr(src: Union[Column, str], pattern: Union[Column, str], idx: Union[Column, int] = 0) -> Column` — Find the 1-based position of the first regex match in a string.
- `fc.text.regexp_replace(src: Union[Column, str], pattern: Union[Column, str], replacement: Union[Column, str]) -> Column` — Replace all occurrences of a pattern with a new string, treating pattern as a regular expression.
- `fc.text.regexp_substr(src: Union[Column, str], pattern: Union[Column, str]) -> Column` — Extract the first substring matching a regex pattern.
- `fc.text.replace(src: Union[Column, str], search: Union[Column, str], replace: Union[Column, str]) -> Column` — Replace all occurrences of a pattern with a new string, treating pattern as a literal string.
- `fc.text.rtrim(col: Union[Column, str]) -> Column` — Remove whitespace from the end of strings in a column.
- `fc.text.split(src: Union[Column, str], pattern: str, limit: int = -1) -> Column` — Split a string column into an array using a regular expression pattern.
- `fc.text.split_part(src: Union[Column, str], delimiter: Union[Column, str], part_number: Union[Column, int]) -> Column` — Split a string and return a specific part using 1-based indexing.
- `fc.text.title_case(column: Union[Column, str]) -> Column` — Convert the first character of each word in a string column to uppercase.
- `fc.text.token_chunk(column: Union[Column, str], chunk_size: int, chunk_overlap_percentage: int = 0) -> Column` — Chunks a string column into chunks of a specified size (in tokens) with an optional overlap.
- `fc.text.trim(column: Union[Column, str]) -> Column` — Remove whitespace from both sides of strings in a column.
- `fc.text.upper(column: Union[Column, str]) -> Column` — Convert all characters in a string column to uppercase.
- `fc.text.word_chunk(column: Union[Column, str], chunk_size: int, chunk_overlap_percentage: int = 0) -> Column` — Chunks a string column into chunks of a specified size (in words) with an optional overlap.

## fc.json.\*

- `fc.json.contains(column: Union[Column, str], value: str) -> Column` — Check if a JSON value contains the specified value using recursive deep search.
- `fc.json.get_type(column: Union[Column, str]) -> Column` — Get the JSON type of each value.
- `fc.json.jq(column: Union[Column, str], query: str) -> Column` — Applies a JQ query to a column containing JSON-formatted strings.

## fc.markdown.\*

- `fc.markdown.extract_header_chunks(column: Union[Column, str], header_level: int) -> Column` — Splits markdown documents into logical chunks based on heading hierarchy.
- `fc.markdown.generate_toc(column: Union[Column, str], max_level: Optional[int] = None) -> Column` — Generates a table of contents from markdown headings.
- `fc.markdown.get_code_blocks(column: Union[Column, str], language_filter: Optional[str] = None) -> Column` — Extracts all code blocks from a column of Markdown-formatted strings.
- `fc.markdown.to_json(column: Union[Column, str]) -> Column` — Converts a column of Markdown-formatted strings into a hierarchical JSON representation.

## fc.semantic.\*

- `fc.semantic.analyze_sentiment(column: Union[Column, str], model_alias: Union[str, ModelAlias, None] = None, temperature: float = 0, request_timeout: Optional[float] = None) -> Column` — Analyzes the sentiment of a string column. Returns one of 'positive', 'negative', or 'neutral'.
- `fc.semantic.classify(column: Union[Column, str], classes: Union[List[str], List[ClassDefinition]], examples: Optional[ClassifyExampleCollection] = None, model_alias: Union[str, ModelAlias, None] = None, temperature: float = 0, request_timeout: Optional[float] = None) -> Column` — Classifies a string column into one of the provided classes.
- `fc.semantic.embed(column: Union[Column, str], model_alias: Union[str, ModelAlias, None] = None) -> Column` — Generate embeddings for the specified string column.
- `fc.semantic.extract(column: Union[Column, str], response_format: type[pydantic.main.BaseModel], max_output_tokens: int = 1024, temperature: float = 0.0, model_alias: Union[str, ModelAlias, None] = None, request_timeout: Optional[float] = None) -> Column` — Extracts structured information from unstructured text using a provided Pydantic model schema.
- `fc.semantic.map(prompt: str, /, *, strict: bool = True, examples: Optional[MapExampleCollection] = None, response_format: Optional[type[pydantic.main.BaseModel]] = None, model_alias: Union[str, ModelAlias, None] = None, temperature: float = 0.0, max_output_tokens: int = 512, request_timeout: Optional[float] = None, **columns: Column) -> Column` — Applies a generation prompt to one or more columns, enabling rich summarization and generation tasks.
- `fc.semantic.parse_pdf(column: Union[Column, str], model_alias: Union[str, ModelAlias, None] = None, page_separator: Optional[str] = None, describe_images: bool = False, max_output_tokens: Optional[int] = None, request_timeout: Optional[float] = None) -> Column` — Parses a column of PDF paths into markdown.
- `fc.semantic.predicate(predicate: str, /, *, strict: bool = True, examples: Optional[PredicateExampleCollection] = None, model_alias: Union[str, ModelAlias, None] = None, temperature: float = 0.0, request_timeout: Optional[float] = None, **columns: Column) -> Column` — Applies a boolean predicate to one or more columns, typically used for filtering.
- `fc.semantic.reduce(prompt: str, column: Union[Column, str], *, group_context: Optional[Dict[str, Column]] = None, order_by: List[Union[Column, str]] = None, model_alias: Union[str, ModelAlias, None] = None, temperature: float = 0, max_output_tokens: int = 512, request_timeout: Optional[float] = None) -> Column` — Aggregate function: reduces a set of strings in a column to a single string using a natural language instruction.
- `fc.semantic.summarize(column: Union[Column, str], format: Union[KeyPoints, Paragraph, None] = None, temperature: float = 0, model_alias: Union[str, ModelAlias, None] = None, request_timeout: Optional[float] = None) -> Column` — Summarizes strings from a column.
- `fc.semantic.validate_output_format(model: 'type[BaseModel]') -> 'None'` — Check a Pydantic model type to ensure it is valid schema for semantic operations.

## fc.embedding.\*

- `fc.embedding.compute_similarity(column: Union[Column, str], other: Union[Column, str, List[float], numpy.ndarray], metric: Literal['cosine', 'l2', 'dot'] = 'cosine') -> Column` — Compute similarity between embedding vectors using specified metric.
- `fc.embedding.normalize(column: Union[Column, str]) -> Column` — Normalize embedding vectors to unit length.

## fc.dt.\*

- `fc.dt.current_date() -> Column` — Get the current date.
- `fc.dt.current_timestamp() -> Column` — Get the current date and time.
- `fc.dt.date_add(column: Union[Column, str], days: Union[int, Column, str]) -> Column` — Adds the number of days to the date/timestamp column.
- `fc.dt.date_format(column: Union[Column, str], format: str) -> Column` — Formats a date/timestamp column to a given format.
- `fc.dt.date_sub(column: Union[Column, str], days: Union[int, Column, str]) -> Column` — Subtracts the number of days from the date/timestamp column.
- `fc.dt.date_trunc(column: Union[Column, str], unit: Literal['year', 'month', 'day', 'hour', 'minute', 'second', 'millisecond']) -> Column` — Truncate a date to a given unit.
- `fc.dt.datediff(end: Union[Column, str], start: Union[Column, str]) -> Column` — Calculates the number of days between two date/timestamp columns.
- `fc.dt.day(column: Union[Column, str]) -> Column` — Extract the day from a day column.
- `fc.dt.from_utc_timestamp(column: Union[Column, str], tz: str) -> Column` — Accepts a Column with [TimestampType] (UTC). For each row, converts the timestamp value to the provided `tz` timezone, then renders that timestamp as UTC without changing the timestamp value. In other words, this function shifts the timestamp by the timezone offset `out = t+offset(t+tz)`.
- `fc.dt.hour(column: Union[Column, str]) -> Column` — Extract the hour from a day column.
- `fc.dt.millisecond(column: Union[Column, str]) -> Column` — Extract the hour from a millisecond column.
- `fc.dt.minute(column: Union[Column, str]) -> Column` — Extract the minute from a day column.
- `fc.dt.month(column: Union[Column, str]) -> Column` — Extract the month from a month column.
- `fc.dt.now() -> Column` — Get the current date and time.
- `fc.dt.second(column: Union[Column, str]) -> Column` — Extract the hour from a second column.
- `fc.dt.timestamp_add(column: Union[Column, str], quantity: Union[int, Column, str], unit: Literal['year', 'month', 'day', 'hour', 'minute', 'second', 'millisecond']) -> Column` — Adds the quantity of the given unit to the timestamp column.
- `fc.dt.timestamp_diff(start: Union[Column, str], end: Union[Column, str], unit: Literal['year', 'month', 'day', 'hour', 'minute', 'second', 'millisecond']) -> Column` — Calculates the difference between two timestamp columns.
- `fc.dt.to_date(column: Union[Column, str], format: Optional[str] = None) -> Column` — Transform a string into a DateType.
- `fc.dt.to_timestamp(column: Union[Column, str], format: Optional[str] = None) -> Column` — Transform a string into a TimestampType.
- `fc.dt.to_utc_timestamp(column: Union[Column, str], tz: str) -> Column` — Accepts a Column with [TimestampType] (UTC), interprets each value as wall-clock time in the specified timezone `tz`, and converts it to a timestamp in UTC.
- `fc.dt.year(column: Union[Column, str]) -> Column` — Extract the year from a date column.

## fc.arr.\*

- `fc.arr.compact(column: Union[Column, str]) -> Column` — Removes null values from an array.
- `fc.arr.contains(column: Union[Column, str], value: Union[str, int, float, bool, Column]) -> Column` — Checks if array column contains a specific value.
- `fc.arr.distinct(column: Union[Column, str]) -> Column` — Removes duplicate values from an array column.
- `fc.arr.element_at(column: Union[Column, str], index: Union[int, Column, str]) -> Column` — Returns the element at the given index in an array using 1-based indexing (PySpark compatible).
- `fc.arr.except_(col1: Union[Column, str], col2: Union[Column, str]) -> Column` — Returns elements in the first array but not in the second.
- `fc.arr.intersect(col1: Union[Column, str], col2: Union[Column, str]) -> Column` — Returns the intersection of two arrays.
- `fc.arr.lit(value: Any) -> Column` — Creates a Column expression representing a literal value.
- `fc.arr.max(column: Union[Column, str]) -> Column` — Returns the maximum value in an array.
- `fc.arr.min(column: Union[Column, str]) -> Column` — Returns the minimum value in an array.
- `fc.arr.overlap(col1: Union[Column, str], col2: Union[Column, str]) -> Column` — Checks if two arrays have at least one common element.
- `fc.arr.remove(column: Union[Column, str], element: Union[str, int, float, bool, Column]) -> Column` — Removes all occurrences of an element from an array.
- `fc.arr.repeat(col: Union[Column, str], count: Union[int, Column, str]) -> Column` — Creates an array containing the element repeated count times.
- `fc.arr.reverse(column: Union[Column, str]) -> Column` — Reverses the elements of an array.
- `fc.arr.size(column: Union[Column, str]) -> Column` — Returns the number of elements in an array column.
- `fc.arr.slice(column: Union[Column, str], start: Union[int, Column, str], length: Union[int, Column, str]) -> Column` — Extracts a subarray from an array using 1-based indexing (PySpark compatible).
- `fc.arr.sort(column: Union[Column, str]) -> Column` — Sorts the array in ascending order.
- `fc.arr.union(col1: Union[Column, str], col2: Union[Column, str]) -> Column` — Returns the union of two arrays without duplicates.

## fc.\* (top-level functions)

- `fc.approx_count_distinct(column: Union[Column, str]) -> Column` — Aggregate function: returns an approximate count (HyperLogLog++) of distinct non-null values.
- `fc.array(*args: Union[Column, str, List[Union[Column, str]], Tuple[Union[Column, str], ...]]) -> Column` — Creates a new array column from multiple input columns.
- `fc.array_agg(column: Union[Column, str]) -> Column` — Alias for collect_list().
- `fc.array_contains(column: Union[Column, str], value: Union[str, int, float, bool, Column]) -> Column` — Checks if array column contains a specific value.
- `fc.array_size(column: Union[Column, str]) -> Column` — Returns the number of elements in an array column.
- `fc.asc(column: Union[Column, str]) -> Column` — Mark this column for ascending sort order with nulls first.
- `fc.asc_nulls_first(column: Union[Column, str]) -> Column` — Alias for asc().
- `fc.asc_nulls_last(column: Union[Column, str]) -> Column` — Mark this column for ascending sort order with nulls last.
- `fc.async_udf(f: Optional[Callable[..., Awaitable[Any]]] = None, *, return_type: DataType, max_concurrency: int = 10, timeout_seconds: float = 30, num_retries: int = 0)` — A decorator for creating async user-defined functions (UDFs) with configurable concurrency and retries.
- `fc.avg(column: Union[Column, str]) -> Column` — Aggregate function: returns the average (mean) of all values in the specified column. Applies to numeric and embedding types.
- `fc.coalesce(*cols: Union[Column, str]) -> Column` — Returns the first non-null value from the given columns for each row.
- `fc.col(col_name: str) -> Column` — Creates a Column expression referencing a column in the DataFrame.
- `fc.collect_list(column: Union[Column, str]) -> Column` — Aggregate function: collects all values from the specified column into a list.
- `fc.configure_logging(log_level: int = 20, log_format: str = '%(asctime)s [%(name)s] %(levelname)s: %(message)s', log_stream: Optional[TextIO] = None) -> None` — Configure logging for the library and root logger in interactive environments.
- `fc.count(column: Union[Column, str]) -> Column` — Aggregate function: returns the count of non-null values in the specified column.
- `fc.count_distinct(*cols: Union[Column, str]) -> Column` — Aggregate function: returns the number of distinct non-null rows across one or more columns.
- `fc.create_mcp_server(session: Session, server_name: str, *, user_defined_tools: Optional[List[UserDefinedTool]] = None, system_tools: Optional[SystemToolConfig] = None, concurrency_limit: int = 8) -> FenicMCPServer` — Create an MCP server from datasets and tools.
- `fc.desc(column: Union[Column, str]) -> Column` — Mark this column for descending sort order with nulls first.
- `fc.desc_nulls_first(column: Union[Column, str]) -> Column` — Alias for desc().
- `fc.desc_nulls_last(column: Union[Column, str]) -> Column` — Mark this column for descending sort order with nulls last.
- `fc.empty(data_type: DataType) -> Column` — Creates a Column expression representing an empty value of the given type.
- `fc.first(column: Union[Column, str]) -> Column` — Aggregate function: returns the first non-null value in the specified column.
- `fc.flatten(column: Union[Column, str]) -> Column` — Flattens an array of arrays into a single array (one level deep).
- `fc.greatest(*cols: Union[Column, str]) -> Column` — Returns the greatest value from the given columns for each row.
- `fc.least(*cols: Union[Column, str]) -> Column` — Returns the least value from the given columns for each row.
- `fc.lit(value: Any) -> Column` — Creates a Column expression representing a literal value.
- `fc.max(column: Union[Column, str]) -> Column` — Aggregate function: returns the maximum value in the specified column.
- `fc.mean(column: Union[Column, str]) -> Column` — Aggregate function: returns the mean (average) of all values in the specified column.
- `fc.min(column: Union[Column, str]) -> Column` — Aggregate function: returns the minimum value in the specified column.
- `fc.null(data_type: DataType) -> Column` — Creates a Column expression representing a null value of the specified data type.
- `fc.run_mcp_server_asgi(server: FenicMCPServer, *, stateless_http: bool = True, transport: Literal['streamable-http', 'sse'] = 'streamable-http', path: Optional[str] = '/mcp', **kwargs)` — Run an MCP server as a Starlette ASGI app.
- `fc.run_mcp_server_async(server: FenicMCPServer, *, transport: Literal['http', 'stdio'] = 'http', stateless_http: bool = True, port: Optional[int] = None, host: Optional[str] = None, path: Optional[str] = '/mcp', **kwargs)` — Run an MCP server asynchronously.
- `fc.run_mcp_server_sync(server: FenicMCPServer, *, transport: Literal['http', 'stdio'] = 'http', stateless_http: bool = True, port: Optional[int] = None, host: Optional[str] = None, path: Optional[str] = '/mcp', **kwargs)` — Run an MCP server synchronously.
- `fc.stddev(column: Union[Column, str]) -> Column` — Aggregate function: returns the sample standard deviation of the specified column.
- `fc.struct(*args: Union[Column, str, List[Union[Column, str]], Tuple[Union[Column, str], ...]]) -> Column` — Creates a new struct column from multiple input columns.
- `fc.sum(column: Union[Column, str]) -> Column` — Aggregate function: returns the sum of all values in the specified column.
- `fc.sum_distinct(column: Union[Column, str]) -> Column` — Aggregate function: returns the sum of distinct numeric values in the specified column.
- `fc.tool_param(parameter_name: str, data_type: DataType) -> Column` — Creates an unresolved literal placeholder column with a declared data type.
- `fc.udf(f: Optional[Callable] = None, *, return_type: DataType)` — A decorator or function for creating user-defined functions (UDFs) that can be applied to DataFrame rows.
- `fc.when(condition: Column, value: Column) -> Column` — Evaluates a conditional expression (like if-then).
